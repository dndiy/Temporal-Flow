# .github/workflows/rebuild.yml
name: Rebuild and Deploy Site

# Controls when the action will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  
  # Runs on pushes to the main branch
  push:
    branches: [ main ]
    paths:
      - 'src/content/**'
      - 'src/config/**'

# Jobs to perform
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository
      - uses: actions/checkout@v3
      
      # Setup Node.jsname: Rebuild and Deploy Site

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'src/content/**'
      - 'src/config/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 1. Install pnpm first
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      # 2. Setup Node.js with pnpm support
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'

      # 3. Install dependencies using pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 4. Build the site
      - name: Build site
        run: pnpm run build

      # 5. Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: gh-pages
          folder: dist
          clean: true
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'pnpm'
      
      # Install dependencies
      - name: Install dependencies
        run: npm ci
      
      # Build the site
      - name: Build site
        run: pnpm run build
      
      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: gh-pages
          folder: dist
          clean: true