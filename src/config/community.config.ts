// Community page configuration

import type { CommunityConfig } from '../types/communityconfig';
import { defaultCommunityConfig } from '../types/communityconfig';

// Export the community configuration
export const communityConfig: CommunityConfig = {
  ...defaultCommunityConfig,
  // Override any default values here
};